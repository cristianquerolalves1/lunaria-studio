---
interface Props {
  class?: string;
}

const baseClass = "text-2xl md:text-5xl font-semibold text-center";
const { class: className } = Astro.props;
const combinedClass = className ?? baseClass;
---

<h2 class={combinedClass}>
  <slot />
</h2>
