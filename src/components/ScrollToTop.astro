<button
  id="scrollTop"
  class="fixed bottom-6 right-6 bg-primary text-white rounded-full w-12 h-12 flex items-center justify-center opacity-0 scale-0 transition-all duration-300 hover:scale-110 shadow-lg"
  title="Volver arriba"
>
  ↑
</button>

<script is:inline>
  const btn = document.getElementById("scrollTop");

  window.addEventListener("scroll", () => {
    if (window.scrollY > 400) {
      btn.classList.add("opacity-100", "scale-100");
    } else {
      btn.classList.remove("opacity-100", "scale-100");
    }
  });

  btn.addEventListener("click", () => {
    window.scrollTo({ top: 0, behavior: "smooth" });
  });
</script>
